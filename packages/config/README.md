# Config package

Future shared configuration helpers.

Expected responsibilities:

- environment parsing;
- safe defaults for local development;
- required variable validation;
- typed configuration exports for apps and workers.
