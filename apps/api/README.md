# API application

Future backend service for the SaaS CRM.

Expected responsibilities:

- authentication and tenant context;
- platform administration API;
- tenant CRM API;
- RBAC and permission checks;
- database migrations;
- audit log;
- integration endpoints;
- health checks.

The framework must be selected after discovery of the existing CRM stack.
