# Infrastructure

Infrastructure files for local development, CI, and deployment.

Current contents:

- `docker/compose.dev.yml` for local PostgreSQL, Redis, and Mailpit.

Future additions:

- CI workflows;
- deployment manifests;
- environment-specific configuration;
- backup and restore scripts.
