# CI

Future CI configuration and notes.

Expected checks after the stack is selected:

- formatting;
- linting;
- unit tests;
- integration tests;
- database migration check;
- security/dependency scan.
