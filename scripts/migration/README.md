# Migration scripts

Future ETL scripts for moving the current CRM data into the SaaS model.

Migration scripts must support:

- dry run;
- validation;
- batch processing;
- resumable execution;
- clear logs.
